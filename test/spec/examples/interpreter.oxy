def interp (value: Str) Str {
    return value;
}

def repl () {
    while {
        let inp = input("> ");
        if inp == "exit" { break }
        print(interp(inp));
    }
}

def main() {
    repl();
}