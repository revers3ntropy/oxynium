func interp (value: Str) Str {
    return value;
}

func repl () {
    while {
        let inp = input("> ");
        if inp == "exit" { break }
        print(interp(inp));
    }
}

func main() {
    repl();
}