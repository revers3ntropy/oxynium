def _LibC.calloc () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call calloc
    "
}

def _LibC.memcpy () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call memcpy
    "
}

def _LibC.free () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call free
    "
}

def _LibC.sprintf () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call sprintf
    "
}

def _LibC.time () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call time
    "
}

def _LibC.fopen () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call fopen
    "
}

def _LibC.fseek () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call fseek
    "
}

def _LibC.fread () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call fread
    "
}

def _LibC.ftell () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call ftell
    "
}

def _LibC.fclose () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call fclose
    "
}

def _LibC.fwrite () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call fwrite
    "
}
