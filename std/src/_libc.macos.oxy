def _LibC.calloc () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _calloc
    "
}

def _LibC.memcpy () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _memcpy
    "
}

def _LibC.free () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _free
    "
}

def _LibC.sprintf () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _sprintf
    "
}

def _LibC.time () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _time
    "
}

def _LibC.fopen () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _fopen
    "
}

def _LibC.fseek () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _fseek
    "
}

def _LibC.fread () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _fread
    "
}

def _LibC.ftell () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _ftell
    "
}

def _LibC.fclose () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _fclose
    "
}

def _LibC.fwrite () {
    #asm Void "
        sub rsp, 32
        and rsp, -16
        call _fwrite
    "
}
