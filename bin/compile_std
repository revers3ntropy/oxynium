#!/bin/bash

{
    rm ./out.asm
    rm ./out.o
    rm ./out
    rm ./std.o
    rm ./std.asm
} 2> /dev/null

# compile, link and assemble the program
export RUST_BACKTRACE=1
cargo run -- -x=1 std/std.oxy

rm oxy-out.o
mv oxy-out.asm std.asm
rm out
